create table professor (id serial, 
			nome varchar(50),
			siape int,
			ramal int,
			celular int,
			sala varchar(20),
			cpf int,
			identidade int, 
			email varchar(50),
			primary key(id));

select*from professor

drop table professor
